// late-night array babysitting: five tiny chores, one overcaffeinated kangaroo brain ( ͡° ͜ʖ ͡°)
// Poznań rain on the sill, Melbourne sun squatting in my head; I press keys, it pretends to be destiny
// rule of the alley: code stays untouched, only my rambling ghosts in the margins

function getLength(arr){
  // count the ghosts in the hallway; arrays never admit how many until you ask nicely
  // some nights it’s full, some nights it’s echo — either way, numbers don’t lie, people do
  // k***a, I love when something just reports the truth without drama
  return arr.length;
}

function getFirst(arr){
  // first to the bar, first to regret; index starts at 0 because life enjoys being awkward
  // if there’s nothing there, JS gives you undefined — like a text that never arrives
  return arr[0];
}

function getLast(arr){
  // last one out turns off the lights; length - 1 is the final tram home
  // I’ve missed that tram before. the code won’t. (ง'̀-'́)ง
  return arr[arr.length - 1];
}

function pushElement(arr){
  var el=1;
  // staple a new memory to the end; promises, ticket stubs, half-warm hopes
  // push returns the new length, but we keep the story and hand it back whole
  arr.push(el);
  return arr;
}

function popElement(arr){
  // snip the last page from the diary; sometimes it’s mercy, sometimes it’s theft
  // pop hands you the removed piece, but we just nod and carry on
  arr.pop();
  return arr;
}

// pause. flashback. boardwalk gulls in St Kilda, neon hum in Poznań, heart somewhere in between.
// code passes, tests smile, and for one quiet minute the hangover shuts up. f*** it, that’s a win (╯°□°）╯︵ ┻━┻

// Havin' fun mate? Make me have fun as well.
// I know the page's called Buy Me a Coffee, but I won't lie.
// Buy me a beer: https://buymeacoffee.com/k4ng00r