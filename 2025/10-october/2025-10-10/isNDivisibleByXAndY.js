// Two gates, one number: the bouncer checks both stamps before you get in.
// Some nights one wristband works; not this one. Both or back to the queue. ( ͡° ͜ʖ ͡°)
const isDivisible = (n, x, y) => n % x === 0 && n % y === 0;
// Mini-timeline: plan → gate X → gate Y → either tram home or straight to the dance floor.