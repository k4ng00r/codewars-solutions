/*
  // 🦘 Kangaroo’s Reverse Reality //

  Reverse a string, huh? Easy for code — harder for life.  
  I’ve spent years trying to reverse mine:  
  from Poznań’s cold benches back to Melbourne’s warm chaos.  
  From cheap vodka and ashtrays to clean coffee and syntax.  
  But life doesn’t have a built-in .reverse() mate,  
  only late regrets and early hangovers.

  My mate said coding pays, so here I am —  
  flipping words instead of coins, trying to make sense of both sides.  
  Didn’t check justjoinit, didn’t care about the numbers.  
  I just know that if I keep coding,  
  maybe one day I’ll reverse my way home.
*/

function solution(str){
  // Kangaroo’s magic trick — take the chaos, break it down, flip it, and glue it back together.
  // First, .split("") tears the string apart, like my first marriage.
  // Then .reverse() spins it around — dizzy but alive.
  // Finally, .join("") stitches it back into something that almost looks right again.
  return str.split("").reverse().join("");
}